[phases.setup]
nixPkgs = ["nodejs_20", "npm", "postgresql"]

[phases.install]
cmds = [
  "npm ci --omit=dev"
]

[phases.build]
cmds = [
  "npx nx build backend"
]

[start]
cmd = "node apps/backend/dist/main.js"

[variables]
NODE_ENV = "production"
PORT = "3000"
