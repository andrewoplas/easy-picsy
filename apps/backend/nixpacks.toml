[phases.setup]
nixPkgs = ["nodejs_18", "npm", "postgresql"]

[phases.install]
cmds = [
  "npm ci --only=production"
]

[phases.build]
cmds = [
  "cd apps/backend && npm run build"
]

[start]
cmd = "node apps/backend/dist/main.js"

[variables]
NODE_ENV = "production"
PORT = "3000"
